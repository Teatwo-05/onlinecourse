# Bật engine rewrite
RewriteEngine On

# Nếu yêu cầu là một file hoặc thư mục thực tế, dừng việc rewrite (cho phép truy cập trực tiếp)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Chuyển hướng tất cả các yêu cầu còn lại đến index.php
# QSA (Query String Append): Đảm bảo các tham số có sẵn vẫn được giữ lại (nếu có)
RewriteRule ^ index.php [L,QSA]